import { CardsfilterPipe } from './cardsfilter.pipe';

describe('CardsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
